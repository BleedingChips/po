#include "dx11_define.h"
#include "../../frame/define.h"
#include <fstream>
namespace PO
{
	namespace Dx11
	{
		/*
		HRESULT shader_v::load_binary(const Implement::resource& re, Implement::data d)
		{
			shader = nullptr;
			da = std::move(d);
			return re->CreateVertexShader(da, da.size(), nullptr, &shader);
		}

		HRESULT shader_p::load_binary(const Implement::resource& re, Implement::data d)
		{
			shader = nullptr;
			da = std::move(d);
			return re->CreatePixelShader(da, da.size(), nullptr, &shader);
		}
		*/
	}
}